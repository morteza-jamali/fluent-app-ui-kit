import React from 'react';
import { Titlebar } from './titlebar';

// sets the Component preview in gallery view
export const BasicTitlebar = () => {
  return <Titlebar>hello from Titlebar</Titlebar>;
};
